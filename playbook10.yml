---
- name: Implementing host scope variables
  hosts: 172.31.45.223
  tasks:
    - name: Create user
      user:
       name: "{{a}}"
       password: "{{b}}"
       home: "{{c}}"
       uid: "{{d}}" 
       shell: "{{e}}"
       comment: "{{f}}"

...

